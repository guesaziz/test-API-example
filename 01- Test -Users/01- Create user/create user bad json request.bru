meta {
  name: create user bad json request
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/users
  body: json
  auth: none
}

params:query {
  ~page: 1
}

body:json {
  {
      empty 
    
  }
}

assert {
  res.status: eq 400
  res.body.error: contains Invalid
  res.body.message: contains JSON
}

settings {
  encodeUrl: true
  timeout: 0
}
