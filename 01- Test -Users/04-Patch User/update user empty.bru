meta {
  name: update user empty
  type: http
  seq: 4
}

patch {
  url: {{base_url}}/api/users/:id
  body: json
  auth: none
}

params:path {
  id: {{user-id}}
}

body:json {
  {
  
  }
  
}

assert {
  res.status: eq 200
  res.body.updatedAt: contains "{{current_date}}"
}

settings {
  encodeUrl: true
  timeout: 0
}
