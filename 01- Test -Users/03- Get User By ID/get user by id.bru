meta {
  name: get user by id
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/users/:id
  body: none
  auth: none
}

params:path {
  id: {{user-id}}
}

assert {
  res.status: eq 200
  res.body.data.id: eq {{user-id}}
}

settings {
  encodeUrl: true
  timeout: 0
}
